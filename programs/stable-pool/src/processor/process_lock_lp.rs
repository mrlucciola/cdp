use anchor_lang::prelude::*;

use crate::{
    states::*,
    error::*,
    constant::*,
    instructions::*
};

pub fn process_lock_lp(ctx: &Context<LockLp>) -> ProgramResult {
    
    Ok(())
}
